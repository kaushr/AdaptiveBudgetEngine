# Local-only files (handoff notes, scratch) — not for the remote
local/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Secrets / credentials — never commit
*.p8
*.pem
.env

# OS
.DS_Store

# Intermediate call cache (results CSVs are the shipped fallback)
data/results/call_cache.json

# Streamlit local secrets
.streamlit/secrets.toml
